# Copied into each app's own .env. See apps/*/.env.example for the variant that
# gets loaded by the runtime (Expo uses EXPO_PUBLIC_*, Vite uses VITE_*).
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
